
// 
// This file has been automatically generated by RaQt_maker V0.0.7.
// To modify and regenerate it, see the source code available here: 
//         https://github.com/yguillemot/RaQt_maker
// 

#include "QGraphicsView.hpp"

void * QWQGraphicsViewCtor_1(void * parent)
{
    QWidget * xparent = reinterpret_cast<QWidget *>(parent);
    QGraphicsView * ptr = new QGraphicsView(xparent);
    return reinterpret_cast<void *>(ptr);
}

void * SCWQGraphicsViewCtor_1(void * parent)
{
    QWidget * xparent = reinterpret_cast<QWidget *>(parent);
    SCQGraphicsView * ptr = new SCQGraphicsView(xparent);
    return reinterpret_cast<void *>(ptr);
}

void * QWQGraphicsViewCtor_2(void * scene, void * parent)
{
    QGraphicsScene * xscene = reinterpret_cast<QGraphicsScene *>(scene);
    QWidget * xparent = reinterpret_cast<QWidget *>(parent);
    QGraphicsView * ptr = new QGraphicsView(xscene, xparent);
    return reinterpret_cast<void *>(ptr);
}

void * SCWQGraphicsViewCtor_2(void * scene, void * parent)
{
    QGraphicsScene * xscene = reinterpret_cast<QGraphicsScene *>(scene);
    QWidget * xparent = reinterpret_cast<QWidget *>(parent);
    SCQGraphicsView * ptr = new SCQGraphicsView(xscene, xparent);
    return reinterpret_cast<void *>(ptr);
}

void * QWQGraphicsViewsizeHint(void * obj)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QSize retVal = ptr->sizeHint();
    QSize * xxretVal = new QSize(retVal);
    void * xretVal = reinterpret_cast<void *>(xxretVal);
    return xretVal;
}

int8_t QWQGraphicsViewisInteractive(void * obj)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    bool retVal = ptr->isInteractive();
    return retVal;
}

void * QWQGraphicsViewscene(void * obj)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QGraphicsScene * retVal = ptr->scene();
    return retVal;
}

void QWQGraphicsViewsetScene(void * obj, void * scene)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QGraphicsScene * xscene = reinterpret_cast<QGraphicsScene *>(scene);
    ptr->setScene(xscene);
}

void * QWQGraphicsViewsceneRect(void * obj)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QRectF retVal = ptr->sceneRect();
    QRectF * xxretVal = new QRectF(retVal);
    void * xretVal = reinterpret_cast<void *>(xxretVal);
    return xretVal;
}

void QWQGraphicsViewsetSceneRect_1(void * obj, void * rect)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QRectF & xrect = * reinterpret_cast<QRectF *>(rect);
    ptr->setSceneRect(xrect);
}

void QWQGraphicsViewsetSceneRect_2(void * obj, double  x, double  y, double  w, double  h)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    qreal xx = x;
    qreal xy = y;
    qreal xw = w;
    qreal xh = h;
    ptr->setSceneRect(xx, xy, xw, xh);
}

int8_t QWQGraphicsViewisTransformed(void * obj)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    bool retVal = ptr->isTransformed();
    return retVal;
}

void QWQGraphicsViewcenterOn_1(void * obj, void * pos)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QPointF & xpos = * reinterpret_cast<QPointF *>(pos);
    ptr->centerOn(xpos);
}

void QWQGraphicsViewcenterOn_2(void * obj, double  x, double  y)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    qreal xx = x;
    qreal xy = y;
    ptr->centerOn(xx, xy);
}

void QWQGraphicsViewcenterOn_3(void * obj, void * item)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QGraphicsItem * xitem = reinterpret_cast<QGraphicsItem *>(item);
    ptr->centerOn(xitem);
}

void QWQGraphicsViewensureVisible_1(void * obj, void * rect, int32_t  xmargin, int32_t  ymargin)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QRectF & xrect = * reinterpret_cast<QRectF *>(rect);
    int xxmargin = xmargin;
    int xymargin = ymargin;
    ptr->ensureVisible(xrect, xxmargin, xymargin);
}

void QWQGraphicsViewensureVisible_2(void * obj, double  x, double  y, double  w, double  h, int32_t  xmargin, int32_t  ymargin)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    qreal xx = x;
    qreal xy = y;
    qreal xw = w;
    qreal xh = h;
    int xxmargin = xmargin;
    int xymargin = ymargin;
    ptr->ensureVisible(xx, xy, xw, xh, xxmargin, xymargin);
}

void QWQGraphicsViewensureVisible_3(void * obj, void * item, int32_t  xmargin, int32_t  ymargin)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QGraphicsItem * xitem = reinterpret_cast<QGraphicsItem *>(item);
    int xxmargin = xmargin;
    int xymargin = ymargin;
    ptr->ensureVisible(xitem, xxmargin, xymargin);
}

void QWQGraphicsViewfitInView_1(void * obj, void * rect, int  aspectRadioMode)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QRectF & xrect = * reinterpret_cast<QRectF *>(rect);
    Qt::AspectRatioMode xaspectRadioMode = static_cast<Qt::AspectRatioMode>(aspectRadioMode);
    ptr->fitInView(xrect, xaspectRadioMode);
}

void QWQGraphicsViewfitInView_2(void * obj, double  x, double  y, double  w, double  h, int  aspectRadioMode)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    qreal xx = x;
    qreal xy = y;
    qreal xw = w;
    qreal xh = h;
    Qt::AspectRatioMode xaspectRadioMode = static_cast<Qt::AspectRatioMode>(aspectRadioMode);
    ptr->fitInView(xx, xy, xw, xh, xaspectRadioMode);
}

void * QWQGraphicsViewitemAt_1(void * obj, void * pos)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QPoint & xpos = * reinterpret_cast<QPoint *>(pos);
    QGraphicsItem * retVal = ptr->itemAt(xpos);
    return retVal;
}

void * QWQGraphicsViewitemAt_2(void * obj, int32_t  x, int32_t  y)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    int xx = x;
    int xy = y;
    QGraphicsItem * retVal = ptr->itemAt(xx, xy);
    return retVal;
}

void * QWQGraphicsViewmapFromScene_1(void * obj, void * point)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QPointF & xpoint = * reinterpret_cast<QPointF *>(point);
    QPoint retVal = ptr->mapFromScene(xpoint);
    QPoint * xxretVal = new QPoint(retVal);
    void * xretVal = reinterpret_cast<void *>(xxretVal);
    return xretVal;
}

void * QWQGraphicsViewbackgroundBrush(void * obj)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QBrush retVal = ptr->backgroundBrush();
    QBrush * xxretVal = new QBrush(retVal);
    void * xretVal = reinterpret_cast<void *>(xxretVal);
    return xretVal;
}

void QWQGraphicsViewinvalidateScene(void * obj, void * rect, int  layers)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    QRectF & xrect = * reinterpret_cast<QRectF *>(rect);
    QGraphicsScene::SceneLayers xlayers = static_cast<QGraphicsScene::SceneLayers>(layers);
    ptr->invalidateScene(xrect, xlayers);
}

void QWvalidateCB_QGraphicsView(void *obj, int32_t objId, char *methodName)
{
    SCQGraphicsView * ptr = reinterpret_cast<SCQGraphicsView *>(obj);

    ptr->validateEvent(objId, methodName);

}
void QWQGraphicsViewDtor(void * obj)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    delete ptr;
}

void SCWQGraphicsViewDtor(void * obj)
{
    QGraphicsView * ptr = reinterpret_cast<QGraphicsView *>(obj);
    delete ptr;
}

