
// 
// This file has been automatically generated by RaQt_maker V0.0.6.
// To modify and regenerate it, see the source code available here: 
//         https://github.com/yguillemot/RaQt_maker
// 

#include "QBoxLayout.hpp"

void QWQBoxLayoutaddLayout(void * obj, void * layout, int32_t  stretch)
{
    QBoxLayout * ptr = reinterpret_cast<QBoxLayout *>(obj);
    QLayout * xlayout = reinterpret_cast<QLayout *>(layout);
    int xstretch = stretch;
    ptr->addLayout(xlayout, xstretch);
}

int32_t QWQBoxLayoutspacing(void * obj)
{
    QBoxLayout * ptr = reinterpret_cast<QBoxLayout *>(obj);
    int retVal = ptr->spacing();
    return retVal;
}

void QWQBoxLayoutsetSpacing(void * obj, int32_t  spacing)
{
    QBoxLayout * ptr = reinterpret_cast<QBoxLayout *>(obj);
    int xspacing = spacing;
    ptr->setSpacing(xspacing);
}

int8_t QWQBoxLayoutsetStretchFactor_1(void * obj, void * w, int32_t  stretch)
{
    QBoxLayout * ptr = reinterpret_cast<QBoxLayout *>(obj);
    QWidget * xw = reinterpret_cast<QWidget *>(w);
    int xstretch = stretch;
    bool retVal = ptr->setStretchFactor(xw, xstretch);
    return retVal;
}

int8_t QWQBoxLayoutsetStretchFactor_2(void * obj, void * l, int32_t  stretch)
{
    QBoxLayout * ptr = reinterpret_cast<QBoxLayout *>(obj);
    QLayout * xl = reinterpret_cast<QLayout *>(l);
    int xstretch = stretch;
    bool retVal = ptr->setStretchFactor(xl, xstretch);
    return retVal;
}

void QWQBoxLayoutsetStretch(void * obj, int32_t  index, int32_t  stretch)
{
    QBoxLayout * ptr = reinterpret_cast<QBoxLayout *>(obj);
    int xindex = index;
    int xstretch = stretch;
    ptr->setStretch(xindex, xstretch);
}

int32_t QWQBoxLayoutstretch(void * obj, int32_t  index)
{
    QBoxLayout * ptr = reinterpret_cast<QBoxLayout *>(obj);
    int xindex = index;
    int retVal = ptr->stretch(xindex);
    return retVal;
}

